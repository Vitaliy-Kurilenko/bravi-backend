server:
  port: 8083
  servlet:
    context-path: /api

spring:
  application:
    name: bravi
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
